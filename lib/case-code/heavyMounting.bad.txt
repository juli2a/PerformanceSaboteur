// components/inventory/ProductTable.tsx
// all 2000+ rows, not a windowed slice
const rows = table.getRowModel().rows;

{rows.map((row) => (
  <ProductTableRow
    key={row.original.id}
    product={row.original}
    gridTemplateColumns={gridTemplateColumns}
  />
))}

// No windowing (virtualization) at all: all 2000+
// rows mount at once. React's concurrent scheduler
// keeps a single click from freezing the tab
// outright, but it can't make that many real DOM
// subtrees free to build.
